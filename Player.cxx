#include "Player.h"
#include "Renderer.h"

Player::Player() : Object(NULL,NULL) /* FIXME: NULL, NULL for now.. */
{
}

void Player::render(Renderer* renderer) {
    /* Do nothing, because we don't render the player */
}
